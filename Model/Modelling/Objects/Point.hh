#pragma once

#include <vector>
#include "glm/glm.hpp"

using namespace std;
using namespace glm;

struct Point
{
    vec3 position;
    vec3 color;
    vec3 normal;
    vec3 labels;
};


